// Secrets.swift.template — copy to Secrets.swift and fill in real values.
// Secrets.swift is gitignored; never commit it.
//
// The token is the same PIG_TOKEN used by the collector and the web login
// (~/pigprices/repo/backend/.env on the VPS).

enum Secrets {
  static let apiBase = "https://pig.maytek.co"
  static let token = "change-me"
}